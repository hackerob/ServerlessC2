//Login form submit
const form = document.getElementById('signinForm');
form.addEventListener('submit', login);

const form2 = document.getElementById('resetPassword');
form2.addEventListener('submit', resetPasswordRequired);
